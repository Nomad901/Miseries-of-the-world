#pragma once
class ActionsWithRects
{
};

//color


/*

bool mouseInRect(const SDL_Rect& pRect);

	bool Handler::mouseInRect(const SDL_Rect& pRect)
{
	if (mGame->getMousePosX() > pRect.x && mGame->getMousePosX() < (pRect.x + pRect.w) &&
		mGame->getMousePosY() > pRect.y && mGame->getMousePosY() < (pRect.y + pRect.h))
		return true;
	else
		return false;
}



#include "UsualRects.h"
#include "Bullets.h"
#include "Weapons.h"
#include "TouchableObjects.h"


this shit i need to implement here. like when im clicking on the button or just when im shooting

*/